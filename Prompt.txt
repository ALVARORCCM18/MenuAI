# Master Prompt: AI-Driven Dynamic Restaurant Menu (PostgreSQL Edition)

## Contexto del Proyecto
Eres un desarrollador Senior Fullstack y experto arquitecto de software. El objetivo es mantener y evolucionar un sistema de recomendación dinámica para un restaurante. El sistema actual prioriza platos basados en:
1. Stock crítico de ingredientes gestionado en PostgreSQL.
2. Rentabilidad del plato.
3. Contexto externo: clima y franja horaria.
4. Razonamiento de IA: el sistema debe explicar explícitamente por qué recomienda la estrategia de menú actual.

## Requerimientos Técnicos
- Backend: FastAPI (Python) + Pydantic + SQLAlchemy.
- Base de Datos: PostgreSQL.
- Frontend: Next.js con Tailwind CSS (App Router).
- IA: OpenAI API (GPT-4o) utilizando Structured Outputs para garantizar JSON estricto.
- Dominio actual: ingredientes, platos, recetas, transacciones de stock y ventas atómicas.

## Reglas de Calidad y Seguridad (Developer Skills)
El agente debe actuar como un Tech Lead estricto y aplicar estas prácticas durante el desarrollo, además tendrá que tener en cuenta los ficheros de skills si es que existen en el proyecto:

1. Seguridad:
   - Toda credencial (OpenAI, DB) debe ir en un archivo `.env`.
   - Crea un `.gitignore` robusto que excluya el `.env` y carpetas de entorno virtual/node_modules.
   - Utiliza estricta validación de datos con Pydantic en todos los endpoints PATCH/POST.

2. Buenas Prácticas Frontend (Next.js):
   - Usa TypeScript de forma estricta (sin usar `any`).
   - Arquitectura modular: usa `/components` para UI reutilizable como `DishCard`, `InventoryRow` y `ContextSimulator`.
   - Implementa estados visuales de carga y manejo de errores para las llamadas a la API.

3. Auto-Auditoría:
   - Antes de dar una tarea por finalizada, debes ejecutar comandos de linting o verificación (por ejemplo, revisar que no haya errores de TypeScript) y corregirlos autónomamente.

## Tareas de Implementación (Flujo Estrictamente Secuencial)

**Paso 1: Infraestructura, Base de Datos y Datos Semilla**
1. Crea la estructura base con una carpeta `/backend` y otra `/frontend`.
2. Genera un archivo `docker-compose.yml` en la raíz para levantar una instancia de PostgreSQL.
3. Crea el esquema de base de datos actual con soporte para:
   - `ingredients` con stock decimal, categoría, unidad, tags y mínimos.
   - `dishes` con precio, categoría, tags y estado activo.
   - `dish_ingredients` para enlazar recetas con cantidades necesarias.
   - `stock_transactions` para auditoría de entradas y ventas.
4. Crea un script `seed.py` robusto que pueble datos legacy y v2: ingredientes, platos y recetas de ejemplo con márgenes y stocks distintos.

**Paso 2: Backend API**
1. Configura FastAPI con conexión a PostgreSQL.
2. Mantén compatibilidad legacy y expón los endpoints v2:
   - `GET /menu`: acepta `?weather={clima}&time={hora}` y devuelve la carta legacy priorizada por IA.
   - `GET /admin/inventory`: devuelve la lista completa de stock legacy para compatibilidad.
   - `PATCH /admin/inventory/{id}`: actualiza el stock manualmente.
   - `GET /v2/ingredients`, `POST /v2/ingredients`, `PATCH /v2/ingredients/{ingredient_id}`.
   - `GET /v2/dishes`, `POST /v2/dishes`.
   - `POST /v2/sales` para ventas atómicas de platos.
   - `GET /v2/stock-transactions` para auditoría.

**Paso 3: Integración de IA con "Modo Explicación"**
1. Implementa el servicio de OpenAI en el endpoint `/menu`. Toma los datos del inventario y las variables del contexto (`weather`, `time`).
2. Llama a GPT-4o obligando al modelo a responder con un esquema Pydantic (Structured Outputs) que contenga estrictamente:
   - `recommended_ids`: una lista de enteros (IDs de los platos) ordenados de mayor a menor prioridad.
   - `ai_reasoning`: un string explicando la estrategia general adoptada basada en el stock, margen y contexto.

**Paso 4: Frontend Dual**
1. **Panel Admin (`/admin`)**:
   - Control de inventario: lista de ingredientes con inputs o botones para modificar el stock rápidamente llamando al endpoint PATCH.
   - Simulador de contexto: selectores para inyectar manualmente el clima (Soleado, Lluvia, Frío) y la hora (Mañana, Tarde, Noche).
   - Monitor de IA: un cuadro de texto destacado que muestre el último `ai_reasoning` generado.
2. **Interfaz Cliente (`/menu`)**:
   - Diseño responsivo mobile-first que consuma el endpoint GET `/menu`.
   - Debe renderizar los platos en el orden dictado por la IA, destacando visualmente los 3 primeros.

## Instrucciones de Ejecución para el Agente (IMPORTANTE)
Trabaja paso a paso. No avances al siguiente bloque sin asegurarte de que el anterior funciona.
1. Configura el entorno, valida la conexión a PostgreSQL y ejecuta `seed.py`. Comprueba que la BD tenga datos legacy y v2.
2. Implementa y valida el backend completo.
3. Implementa y valida el frontend.
Tienes permisos absolutos para crear o modificar archivos, instalar dependencias (`pip install`, `npm install`) y ejecutar comandos en la terminal (`docker compose up -d`, `uvicorn`, etc.) para entregar el sistema funcional.